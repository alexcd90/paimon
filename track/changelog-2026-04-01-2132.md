# Apache Paimon 研发进展 - 2026-04-01-2132

> 拉取时间：2026-04-01 21:32
> 新增 commit 数量：16
> commit 范围：5cc4f99d2..56ad27bc9

---

## 变更列表

### 1. Feature | core | Introduce scan.primary-branch which is symmetrical to scan.fallback-branch (#7553)

- **commit**：`6beab2073`（作者：tsreaper，时间：2026-03-31）
- **类型**：Feature
- **摘要**：引入 `scan.primary-branch` 配置项，与已有的 `scan.fallback-branch` 对称。用户可将主键表中的大量数据组织到另一个非主键分支以加速查询，读取时会优先从主键分支（primary branch）获取数据，缺失时回退到 fallback 分支。
- **关键文件**：
  - [`paimon-core/src/main/java/org/apache/paimon/table/FallbackReadFileStoreTable.java`](file:///e:/project/java-project/paimon/paimon-core/src/main/java/org/apache/paimon/table/FallbackReadFileStoreTable.java)：重构 FallbackReadFileStoreTable，支持 primary-branch 逻辑
  - [`paimon-core/src/main/java/org/apache/paimon/table/FileStoreTableFactory.java`](file:///e:/project/java-project/paimon/paimon-core/src/main/java/org/apache/paimon/table/FileStoreTableFactory.java)：工厂类适配新配置创建带 primary-branch 的表
  - [`paimon-common/src/main/java/org/apache/paimon/CoreOptions.java`](file:///e:/project/java-project/paimon/paimon-common/src/main/java/org/apache/paimon/CoreOptions.java)：新增 `scan.primary-branch` 选项定义
  - [`paimon-core/src/main/java/org/apache/paimon/schema/SchemaValidation.java`](file:///e:/project/java-project/paimon/paimon-core/src/main/java/org/apache/paimon/schema/SchemaValidation.java)：新增 schema 验证逻辑

---

### 2. Fix | python | [python] Fix token cache pollution in Python RESTTokenFileIO (#7562)

- **commit**：`8a310149c`（作者：shyjsarah，时间：2026-03-31）
- **类型**：Fix
- **摘要**：修复 Python `RESTTokenFileIO` 中类级别 `_TOKEN_CACHE` 以表标识符为键导致的跨 catalog 实例 token 污染问题。移除类级别缓存及相关锁，改为实例级锁 + 双重检查模式，对齐 Java 实现。同时修复 `refresh_token()` 中系统表标识符处理，在请求 token 前去掉 `$snapshots` 后缀。
- **关键文件**：
  - [`paimon-python/pypaimon/catalog/rest/rest_token_file_io.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/catalog/rest/rest_token_file_io.py)：重构 token 缓存机制，移除类级别缓存
  - [`paimon-python/pypaimon/tests/rest/rest_token_file_io_test.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/tests/rest/rest_token_file_io_test.py)：新增测试覆盖 token 隔离场景

---

### 3. Feature | python | [python] Support rename branch api (#7561)

- **commit**：`37b5cb986`（作者：xuzifu666，时间：2026-03-31）
- **类型**：Feature
- **摘要**：在 Python API 中新增重命名分支（rename branch）功能，扩展 `BranchManager` 接口，实现 `CatalogBranchManager` 和 `FilesystemBranchManager` 两种后端，并完善 `FileStoreTable` 的分支管理入口。同步更新文档。
- **关键文件**：
  - [`paimon-python/pypaimon/branch/branch_manager.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/branch/branch_manager.py)：新增 `rename_branch` 接口定义
  - [`paimon-python/pypaimon/branch/filesystem_branch_manager.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/branch/filesystem_branch_manager.py)：文件系统后端的重命名实现
  - [`paimon-python/pypaimon/branch/catalog_branch_manager.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/branch/catalog_branch_manager.py)：Catalog 后端的重命名实现
  - [`docs/content/pypaimon/python-api.md`](file:///e:/project/java-project/paimon/docs/content/pypaimon/python-api.md)：文档更新

---

### 4. Perf | python | [python] Push down row ranges to vortex reader (#7558)

- **commit**：`89d54197e`（作者：Jingsong Lee，时间：2026-03-31）
- **类型**：Perf
- **摘要**：将行范围（row ranges）下推到 Vortex 读取器层，使用 `vortex_file.scan(indices=...)` 进行原生行过滤，避免全量读取后再过滤，性能提升显著。同时新增 ClickBench 格式基准测试脚本及数据演化测试用例。
- **关键文件**：
  - [`paimon-python/pypaimon/read/reader/format_vortex_reader.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/read/reader/format_vortex_reader.py)：实现行范围下推逻辑
  - [`paimon-python/pypaimon/read/split_read.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/read/split_read.py)：适配下推接口
  - [`paimon-python/pypaimon/benchmark/clickbench_format.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/benchmark/clickbench_format.py)：新增 ClickBench 基准测试脚本

---

### 5. Docs | python | [python] Add doc and refine config for using pyjindosdk in pypaimon (#7565)

- **commit**：`a32b94021`（作者：timmyyao，时间：2026-03-31）
- **类型**：Docs / Feature
- **摘要**：为 pypaimon 中使用 pyjindosdk 新增完整文档，明确 pyjindosdk 已作为默认实现（只要安装了 pyjindosdk 即自动启用），并在文档中介绍了降级回退方案。同时优化 `pyarrow_file_io.py` 配置，完善 file IO 测试。
- **关键文件**：
  - [`docs/content/pypaimon/pyjindosdk-support.md`](file:///e:/project/java-project/paimon/docs/content/pypaimon/pyjindosdk-support.md)：新增 pyjindosdk 使用文档
  - [`paimon-python/pypaimon/filesystem/pyarrow_file_io.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/filesystem/pyarrow_file_io.py)：优化 pyjindosdk 配置逻辑
  - [`paimon-python/pypaimon/common/options/config.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/common/options/config.py)：调整默认配置

---

### 6. Fix | spark | [spark] Fix view resolution for CTE and ORDER BY ordinal (#7552)

- **commit**：`d3126943a`（作者：Zouxxyy，时间：2026-03-31）
- **类型**：Fix
- **摘要**：修复 `PaimonViewResolver` 未对视图解析计划应用 Spark 早期分析规则（`CTESubstitution`、`SubstituteUnresolvedOrdinals`）的问题。这些规则在 Resolution batch 之前的 earlyBatches 中执行，不会对解析阶段注入的计划重新执行，导致含 CTE 的视图报错 `TABLE_OR_VIEW_NOT_FOUND`，以及 `ORDER BY 1` 序号排序结果不正确。
- **关键文件**：
  - [`paimon-spark/paimon-spark-common/src/main/scala/org/apache/paimon/spark/catalyst/analysis/PaimonViewResolver.scala`](file:///e:/project/java-project/paimon/paimon-spark/paimon-spark-common/src/main/scala/org/apache/paimon/spark/catalyst/analysis/PaimonViewResolver.scala)：在视图计划构建前应用 earlyBatches 规则
  - [`paimon-spark/paimon-spark-ut/src/test/scala/org/apache/paimon/spark/sql/PaimonViewTestBase.scala`](file:///e:/project/java-project/paimon/paimon-spark/paimon-spark-ut/src/test/scala/org/apache/paimon/spark/sql/PaimonViewTestBase.scala)：新增 CTE 视图和序号排序视图测试用例

---

### 7. Perf | vortex | [vortex] Use nativeWriter.writeBatchFfi in VortexRecordsWriter (#7567)

- **commit**：`66aea7dda`（作者：Jingsong Lee，时间：2026-04-01）
- **类型**：Perf / Refactor
- **摘要**：将 `VortexRecordsWriter` 的数据写入从基于 Arrow IPC 序列化字节数组的 `writeBatch(byte[])` 改为使用 Arrow C Data Interface（FFI）指针的 `writeBatchFfi(arrayAddr, schemaAddr)`，避免了 IPC 序列化开销，降低跨 JNI 边界的数据传输成本。同时修复了基于 IPC 字节数统计文件大小导致的 rolling 误判，改为始终返回 false 禁用 rolling。
- **关键文件**：
  - [`paimon-vortex/paimon-vortex-format/src/main/java/org/apache/paimon/format/vortex/VortexRecordsWriter.java`](file:///e:/project/java-project/paimon/paimon-vortex/paimon-vortex-format/src/main/java/org/apache/paimon/format/vortex/VortexRecordsWriter.java)：核心写入逻辑改为 FFI 方式
  - [`paimon-vortex/paimon-vortex-jni/src/main/java/dev/vortex/jni/JNIWriter.java`](file:///e:/project/java-project/paimon/paimon-vortex/paimon-vortex-jni/src/main/java/dev/vortex/jni/JNIWriter.java)：新增 `writeBatchFfi` JNI 实现
  - [`paimon-vortex/paimon-vortex-jni/src/main/java/dev/vortex/jni/NativeWriterMethods.java`](file:///e:/project/java-project/paimon/paimon-vortex/paimon-vortex-jni/src/main/java/dev/vortex/jni/NativeWriterMethods.java)：新增 native 方法声明

---

### 8. Feature | flink | [flink] Support dropping primary keys for empty tables in Flink (#7566)

- **commit**：`2c0a791e9`（作者：tsreaper，时间：2026-04-01）
- **类型**：Feature
- **摘要**：在 Flink 中支持对空表删除主键（DROP PRIMARY KEY），与已有的"为空表添加主键"操作对称。在 `SchemaChange` 中新增 `DropPrimaryKey` 变更类型，`SchemaManager` 中校验表必须为空，`FlinkCatalog` 中识别并允许该操作，非空表尝试删除主键时抛出异常。
- **关键文件**：
  - [`paimon-api/src/main/java/org/apache/paimon/schema/SchemaChange.java`](file:///e:/project/java-project/paimon/paimon-api/src/main/java/org/apache/paimon/schema/SchemaChange.java)：新增 `DropPrimaryKey` SchemaChange 类型
  - [`paimon-core/src/main/java/org/apache/paimon/schema/SchemaManager.java`](file:///e:/project/java-project/paimon/paimon-core/src/main/java/org/apache/paimon/schema/SchemaManager.java)：新增空表校验逻辑
  - [`paimon-flink/paimon-flink-common/src/main/java/org/apache/paimon/flink/FlinkCatalog.java`](file:///e:/project/java-project/paimon/paimon-flink/paimon-flink-common/src/main/java/org/apache/paimon/flink/FlinkCatalog.java)：适配 Flink Catalog ALTER TABLE 流程

---

### 9. Docs | index | [index] Fix: add glibc limited for tantivy jni (#7569)

- **commit**：`5ea9713dc`（作者：jerry，时间：2026-04-01）
- **类型**：Docs
- **摘要**：在 Tantivy JNI 的 README 中补充 Linux glibc 兼容性限制说明：`.so` 必须在与目标运行环境 glibc 版本相同或更低的系统上编译，推荐始终在最旧支持的 Linux 发行版（如 CentOS 7）上构建以确保最大兼容性。
- **关键文件**：
  - [`paimon-tantivy/paimon-tantivy-jni/README.md`](file:///e:/project/java-project/paimon/paimon-tantivy/paimon-tantivy-jni/README.md)：新增 glibc 兼容性注意事项

---

### 10. Fix | python | [python] Fix REST signature mismatch by encoding query param values (#7568)

- **commit**：`1e989c81b`（作者：XiaoHongbo，时间：2026-04-01）
- **类型**：Fix
- **摘要**：修复 REST 请求中查询参数包含特殊字符（如 `func%`）时导致签名不匹配引发 401 认证错误的问题。在 `RESTAuthParameter` 的 `__post_init__` 中对参数值进行 URL 编码（`urllib.parse.quote`），确保签名计算与实际请求一致。
- **关键文件**：
  - [`paimon-python/pypaimon/api/typedef.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/api/typedef.py)：新增参数值 URL 编码逻辑
  - [`paimon-python/pypaimon/tests/rest/dlf_signer_test.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/tests/rest/dlf_signer_test.py)：新增特殊字符参数编码测试

---

### 11. Fix | core | [core] Fix that latestSnapshotOfUser might list snapshot directory (#7572)

- **commit**：`1e8a0f16f`（作者：yuzelin，时间：2026-04-01）
- **类型**：Fix
- **摘要**：修复 `latestSnapshotOfUser` 在 checkpoint 时可能触发全量列举 snapshot 目录的问题。原实现先调用 `earliestSnapshotId()` 确定迭代下界，当 EARLIEST hint 并发失效时会回退到 `findByListFiles()` 列举整个目录，在大规模快照（10000+）场景下频繁触发可导致对象存储 QPS 限额超出。修复改为从最新 snapshot 向后遍历，遇到 `FileNotFoundException` 时停止。
- **关键文件**：
  - [`paimon-core/src/main/java/org/apache/paimon/utils/SnapshotManager.java`](file:///e:/project/java-project/paimon/paimon-core/src/main/java/org/apache/paimon/utils/SnapshotManager.java)：优化 `latestSnapshotOfUser` 遍历逻辑，避免调用 `earliestSnapshotId()`

---

### 12. Fix | python | [python] Fix token cache isolation by adding user identity to cache key (#7571)

- **commit**：`50aa0f131`（作者：shyjsarah，时间：2026-04-01）
- **类型**：Fix
- **摘要**：回退 #7562（完全移除类级缓存），改用最小化修复方案：在 `_TOKEN_CACHE` 的缓存 key 中加入用户身份信息，从而隔离不同凭证（AK/SK）对同一张表的 token，同时保留相同用户的 token 复用，避免不必要的刷新请求。
- **关键文件**：
  - [`paimon-python/pypaimon/catalog/rest/rest_token_file_io.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/catalog/rest/rest_token_file_io.py)：缓存 key 加入用户身份，实现 token 隔离
  - [`paimon-python/pypaimon/tests/rest/rest_token_file_io_test.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/tests/rest/rest_token_file_io_test.py)：扩充测试覆盖多用户场景

---

### 13. Perf | arrow | [arrow] Reuse length array to avoid allocate array memory repeatedly (#7573)

- **commit**：`779c84106`（作者：xuzifu666，时间：2026-04-01）
- **类型**：Perf
- **摘要**：`ArrowFieldWriters` 中 `ArrayWriter` 和 `MapWriter` 的长度数组（`int[] lengths`）在每次 `write` 时都重新分配内存，存在 TODO 标注的性能问题。本次将其改为实例变量 `reusableLengths`，按需扩容复用，减少频繁的数组内存分配开销，同时修复 `getArrayChildWriteInfo` 系列方法依赖 `parentLengths.length` 而非实际元素数量的 bug。
- **关键文件**：
  - [`paimon-arrow/src/main/java/org/apache/paimon/arrow/writer/ArrowFieldWriters.java`](file:///e:/project/java-project/paimon/paimon-arrow/src/main/java/org/apache/paimon/arrow/writer/ArrowFieldWriters.java)：复用长度数组，修复 lenSize 传递问题

---

### 14. Fix | python | [python] Fix update_by_row_id conflict when file rolling splits (#7574)

- **commit**：`e17f0f95e`（作者：XiaoHongbo，时间：2026-04-01）
- **类型**：Fix
- **摘要**：修复在大文件上调用 `update_by_arrow_with_row_id` 或 `upsert_by_arrow_with_key` 时，因文件 rolling 分割为多个文件导致 `MERGE INTO` 与 `COMPACT` 冲突的 RuntimeError。修复方案是在 `update_by_row_id` 流程中对每个 `FileStoreWrite` 禁用 rolling（通过 `disable_rolling()` 确保单个 first_row_id 组只输出一个文件）。
- **关键文件**：
  - [`paimon-python/pypaimon/write/table_update_by_row_id.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/write/table_update_by_row_id.py)：禁用 rolling 防止文件分割冲突
  - [`paimon-python/pypaimon/write/file_store_write.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/write/file_store_write.py)：新增 `disable_rolling()` 方法
  - [`paimon-python/pypaimon/tests/table_update_test.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/tests/table_update_test.py)：新增大文件更新测试场景

---

### 15. Feature | python | [python] Introduce Full Text Search and Tantivy index in Python (#7560)

- **commit**：`c08827bd0`（作者：Jingsong Lee，时间：2026-04-01）
- **类型**：Feature
- **摘要**：在 Python 中引入全文搜索（Full Text Search）和 Tantivy 索引能力，新增 `globalindex` 子模块，包含 `TantivyFullTextGlobalIndexReader`、`OffsetGlobalIndexReader` 等完整实现；在 `Table` 接口上暴露 `full_text_search_builder()` 入口；新增 CLI 子命令支持全文搜索；并提供 Java 与 Python 的端到端测试。
- **关键文件**：
  - [`paimon-python/pypaimon/globalindex/tantivy/tantivy_full_text_global_index_reader.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/globalindex/tantivy/tantivy_full_text_global_index_reader.py)：Tantivy 全文索引读取核心实现
  - [`paimon-python/pypaimon/table/source/full_text_search_builder.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/table/source/full_text_search_builder.py)：全文搜索构建器接口
  - [`paimon-python/pypaimon/cli/cli_table.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/cli/cli_table.py)：CLI 全文搜索子命令
  - [`paimon-tantivy/paimon-tantivy-index/src/test/java/org/apache/paimon/tantivy/index/JavaPyTantivyE2ETest.java`](file:///e:/project/java-project/paimon/paimon-tantivy/paimon-tantivy-index/src/test/java/org/apache/paimon/tantivy/index/JavaPyTantivyE2ETest.java)：Java-Python 端到端测试

---

### 16. Test | python | [python] Make mock REST server behave closer to real REST server (#7575)

- **commit**：`56ad27bc9`（作者：XiaoHongbo，时间：2026-04-01）
- **类型**：Test / Fix
- **摘要**：增强 mock REST server 使其行为更贴近真实 REST server：新增基于 `RESTAuthParameter` 和 `RESTAuthFunction` 的真实签名验证逻辑（不再简单返回 true），支持 `AlterDatabaseRequest` 的 `POST` 处理（属性增删），修复 Authorization 请求头大小写不敏感读取问题，并新增 `alter_database` 集成测试用例。
- **关键文件**：
  - [`paimon-python/pypaimon/tests/rest/rest_server.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/tests/rest/rest_server.py)：实现真实签名验证和 alter database 处理
  - [`paimon-python/pypaimon/tests/rest/rest_base_test.py`](file:///e:/project/java-project/paimon/paimon-python/pypaimon/tests/rest/rest_base_test.py)：新增 alter_database 测试用例

---

## 模块统计

| 模块 | 新增 commit 数 | 类型分布 |
|------|--------------|---------|
| python | 9 | Feature×3, Fix×5, Test×1 |
| core | 2 | Feature×1, Fix×1 |
| spark | 1 | Fix×1 |
| flink | 1 | Feature×1 |
| vortex | 1 | Perf×1 |
| arrow | 1 | Perf×1 |
| index (tantivy) | 1 | Docs×1 |

> **类型汇总**：Feature×5, Fix×7, Perf×2, Docs×2（含 Feature+Docs 复合 1 个）
