# Apache Paimon 研发进展 - 2026-03-23 18:00

> 拉取时间：2026-03-23 18:00
> 新增 commit 数量：34
> commit 范围：a7d9de2..16ecc13

---

## 变更列表

### 1. Fix | core | 分区过期时应同步清理 done 分区

- **commit**：`67d29b9`（作者：tsreaper，时间：2026-03-24）
- **类型**：Fix
- **摘要**：分区过期逻辑未清理 `.done` 标记分区，导致 Metastore 中残留大量 `.done` 分区元数据，本次修复一并清除。
- **关键文件**：
  - `paimon-core/.../operation/PartitionExpire.java`：新增 done 分区的清理逻辑
  - `paimon-hive/.../HiveCatalogITCaseBase.java`：补充 Hive Catalog 相关测试

---

### 2. Feature | core | 引入独立的 VectorSearchBuilder API

- **commit**：`0933636`（作者：Jingsong Lee，时间：2026-03-24）
- **类型**：Feature
- **摘要**：将向量检索从 `ReadBuilder` 中解耦，形成独立的 `VectorSearchBuilder` API，引擎可将不同索引文件拆分为独立并发任务执行，同时新增 `VectorScan` / `VectorRead` 接口层。
- **关键文件**：
  - `paimon-core/.../table/source/VectorSearchBuilder.java`：新增向量检索构建器接口（55行）
  - `paimon-core/.../table/source/VectorSearchBuilderImpl.java`：实现类（226行）
  - `paimon-core/.../table/source/VectorScan.java` / `VectorRead.java`：新增扫描与读取接口
  - `paimon-common/.../testvector/TestVectorGlobalIndexer.java`：新增测试向量索引实现

---

### 3. Fix | core | 修复 BTree 全局索引非索引行范围规划问题

- **commit**：`7fadf8b`（作者：YeJunHao，时间：2026-03-24）
- **类型**：Fix
- **摘要**：修复 BTree 全局索引中非索引行在范围规划时的错误逻辑，补充回归测试。
- **关键文件**：
  - `paimon-core/.../globalindex/DataEvolutionBatchScan.java`：修复规划逻辑

---

### 4. Feature | core | BTree 索引构建时写入行范围索引

- **commit**：`6e129dd`（作者：YeJunHao，时间：2026-03-24）
- **类型**：Feature
- **摘要**：在 BTree 全局索引构建过程中，同时写入行范围（row range）索引，为后续的向量检索预过滤提供基础。跨 Flink、Spark 多模块均有适配。
- **关键文件**：
  - `paimon-core/.../globalindex/btree/BTreeGlobalIndexBuilder.java`：核心构建逻辑（+97行）
  - `paimon-flink/.../btree/BTreeIndexTopoBuilder.java`：Flink 侧适配
  - `paimon-spark/.../spark/util/ScanPlanHelper.scala`：Spark 侧适配

---

### 5. Refactor | core/python | 重构标量索引评估逻辑并引入向量检索预过滤

- **commit**：`71932f7`（作者：Jingsong Lee，时间：2026-03-24）
- **类型**：Refactor + Feature
- **摘要**：移除 `GlobalIndexScanBuilder` / `GlobalIndexScanBuilderImpl`，功能内联到 `GlobalIndexScanner`；引入 `VectorScanImpl` / `VectorReadImpl` / `VectorSearchSplit`，支持在向量检索前通过标量索引进行预过滤，并且全局索引命中时不再查询无索引文件，Python 端同步重构。
- **关键文件**：
  - `paimon-core/.../globalindex/GlobalIndexScanner.java`：重命名并增强（原 RowRangeGlobalIndexScanner）
  - `paimon-core/.../table/source/VectorReadImpl.java`：新增（163行）
  - `paimon-core/.../table/source/VectorScanImpl.java`：新增（125行）
  - `paimon-core/.../table/source/VectorSearchSplit.java`：新增中间数据结构（130行）
  - `paimon-python/.../globalindex/global_index_scanner.py`：Python 端同步重构

---

### 6. Fix | spark | 修复聚簇策略自动选择时列数计算错误

- **commit**：`83bc1f3`（作者：Zouxxyy，时间：2026-03-24）
- **类型**：Fix
- **摘要**：`PaimonSparkWriter` 在自动选择聚簇策略时使用了错误的列数，导致策略选择不正确，一行代码修复。
- **关键文件**：
  - `paimon-spark/.../spark/commands/PaimonSparkWriter.scala`：修复列数计算

---

### 7. Feature | python | Python 支持 truncate 操作

- **commit**：`d949af0`（作者：xuzifu666，时间：2026-03-24）
- **类型**：Feature
- **摘要**：Python API 的 `table_commit` 新增 `truncate()` 方法，允许用户通过 Python 直接截断表数据。
- **关键文件**：
  - `paimon-python/.../write/file_store_commit.py`：实现 truncate 逻辑
  - `paimon-python/.../write/table_commit.py`：暴露 truncate 接口

---

### 8. Fix | python | 修复按 Host 名称选择签名算法的错误

- **commit**：`50b8fcd`（作者：tonymtu，时间：2026-03-25）
- **类型**：Fix
- **摘要**：Python REST Catalog 鉴权模块中，根据 Host 名称选择签名算法的逻辑存在错误，一行修复。
- **关键文件**：
  - `paimon-python/.../api/auth/factory.py`：修复算法选择逻辑

---

### 9. Fix | python | 修复 BINARY(N) 类型映射为可变长度二进制

- **commit**：`098ef8e`（作者：XiaoHongbo，时间：2026-03-25）
- **类型**：Fix
- **摘要**：Python 端 `BINARY(N)` 类型被错误映射为固定长度二进制，修复为可变长度；同时修复写入侧对该类型的处理逻辑，新增 Java-Python E2E 测试覆盖。
- **关键文件**：
  - `paimon-python/.../schema/data_types.py`：修复类型映射
  - `paimon-python/.../write/table_write.py`：修复写入处理

---

### 10. Fix | core | 类型转换失败时异常信息携带字段名

- **commit**：`7a76c43`（作者：Jingsong Lee，时间：2026-03-25）
- **类型**：Fix
- **摘要**：`CastFieldGetter` 在类型转换失败时抛出的异常不含字段名，难以定位问题，本次修复在异常信息中包含字段名，方便用户排查。
- **关键文件**：
  - `paimon-core/.../casting/CastFieldGetter.java`：异常信息增加字段名
  - `paimon-core/.../schema/SchemaEvolutionUtil.java`：配合调整

---

### 11. Feature | python | TableUpsertByKey 重复 key 自动去重保留最后一条

- **commit**：`66454d9`（作者：Jingsong Lee，时间：2026-03-25）
- **类型**：Feature
- **摘要**：`TableUpsertByKey` 对重复 key 的处理从抛异常改为自动去重（保留最后一条），行为更符合 Upsert 语义，减少用户使用负担。
- **关键文件**：
  - `paimon-python/.../write/table_upsert_by_key.py`：更改去重逻辑

---

### 12. Perf | core | 无索引文件时跳过 GlobalIndexScanner 创建

- **commit**：`fe32969`（作者：Jingsong Lee，时间：2026-03-25）
- **类型**：Perf
- **摘要**：当没有全局索引文件时，直接跳过 `GlobalIndexScanner` 的创建，减少不必要的对象初始化开销，Python 端同步适配。
- **关键文件**：
  - `paimon-core/.../globalindex/DataEvolutionBatchScan.java`：短路逻辑

---

### 13. Feature | core | 支持配置压缩文件数上限并在批作业中上报是否达到限制

- **commit**：`2f647dd`（作者：yuzelin，时间：2026-03-25）
- **类型**：Feature
- **摘要**：新增配置项允许限制单次压缩处理的文件数；批作业完成后向外部系统汇报本次压缩是否已处理全部文件，外部系统据此决定是否需要再次触发压缩。
- **关键文件**：
  - `paimon-api/.../CoreOptions.java`：新增压缩文件数上限配置项
  - `paimon-core/.../append/AppendCompactCoordinator.java`：实现上限控制与上报逻辑

---

### 14. Feature | python | Python FileStoreTable 新增 comment 方法

- **commit**：`559b443`（作者：xuzifu666，时间：2026-03-25）
- **类型**：Feature
- **摘要**：Python API 的 `file_store_table` 新增 `comment()` 方法，允许用户直接获取表注释。
- **关键文件**：
  - `paimon-python/.../table/file_store_table.py`：新增 comment 方法

---

### 15. Fix | core | 修复 IndexFileHandler 删除快照时未处理所有索引类型

- **commit**：`0ead733`（作者：jerry，时间：2026-03-25）
- **类型**：Fix
- **摘要**：`IndexFileHandler` 在快照过期清理时仅处理了部分索引类型，导致某些全局索引文件未被删除造成存储泄漏，本次修复支持删除所有索引类型。
- **关键文件**：
  - `paimon-core/.../index/IndexFileHandler.java`：支持所有索引类型删除
  - `paimon-core/.../index/IndexFileHandlerTest.java`：新增测试（105行）

---

### 16. Feature | spark | PartitionedFormatTable 支持 OVERWRITE_BY_FILTER（Spark 4 兼容）

- **commit**：`c37503a`（作者：Kerwin Zhang，时间：2026-03-25）
- **类型**：Feature
- **摘要**：为 `PartitionedFormatTable` 实现 `OVERWRITE_BY_FILTER` 能力，满足 Spark 4 兼容性要求。
- **关键文件**：
  - `paimon-spark/.../sql/execution/SparkFormatTable.scala`：新增 OVERWRITE_BY_FILTER 实现（36行）

---

### 17. Other | release | 版本升级至 1.5-SNAPSHOT

- **commit**：`81b7cc6`（作者：xiaohongbo，时间：2026-03-25）
- **类型**：Release
- **摘要**：全量 pom.xml 版本号升级为 1.5-SNAPSHOT，76 个文件同步变更。

---

### 18. Feature | flink/spark | path_to_descriptor 函数支持 HTTP URL

- **commit**：`e603e4a`（作者：Faiz，时间：2026-03-25）
- **类型**：Feature
- **摘要**：`path_to_descriptor` 函数从仅支持本地/HDFS路径扩展为同时支持 HTTP URL，Flink 和 Spark 两侧均适配。
- **关键文件**：
  - `paimon-flink/.../function/PathToDescriptor.java`：支持 HTTP URL
  - `paimon-spark/.../function/PathToDescriptorFunction.java`：同步适配

---

### 19. Feature | core | 为 pk-clustering-override 添加参数校验

- **commit**：`411195c`（作者：Jingsong Lee，时间：2026-03-25）
- **类型**：Feature
- **摘要**：在 Schema 校验阶段对 `pk-clustering-override` 配置项增加合法性检查，避免用户配置错误导致运行时异常。
- **关键文件**：
  - `paimon-core/.../schema/SchemaValidation.java`：新增校验逻辑（40行）
  - `paimon-core/.../clustering/ClusteringCompactManagerFactory.java`：精简原有逻辑

---

### 20. Feature | core | Chain 表支持 group partition

- **commit**：`f261a66`（作者：Wenchao Wu，时间：2026-03-26）
- **类型**：Feature
- **摘要**：Chain 表原先假设所有分区字段都参与链式维度，导致多分区字段时锚点查找可能跨分区污染。本次引入 group partition 概念，将非链式前缀字段作为分组 key，Delta 分区按 group key 分组后再做链式合并，确保锚点搜索在正确的分区范围内进行。
- **关键文件**：
  - `paimon-core/.../utils/ChainPartitionProjector.java`：新增分区投影工具（160行）
  - `paimon-core/.../utils/ChainTableUtils.java`：新增 Chain 表工具类（132行）
  - `paimon-core/.../table/ChainGroupReadTable.java`：核心读取逻辑重构
  - `paimon-spark/.../SparkChainTableITCase.java`：大型集成测试（1456行）

---

### 21. Feature | core/vector | Lumina 索引写入器添加日志

- **commit**：`34df0ce`（作者：jerry，时间：2026-03-26）
- **类型**：Feature
- **摘要**：为 `LuminaVectorGlobalIndexWriter` 添加详细日志输出，便于生产环境诊断向量索引写入过程。
- **关键文件**：
  - `paimon-lumina/.../index/LuminaVectorGlobalIndexWriter.java`：新增日志（+41行）

---

### 22. Other | release | pypaimon 版本升级至 1.5.dev

- **commit**：`9dffa86`（作者：Jingsong Lee，时间：2026-03-26）
- **类型**：Release
- **摘要**：Python 包版本号升级至 1.5.dev。

---

### 23. Perf | core | global index 构建中 rowsPerShard 作为硬约束

- **commit**：`f7ded22`（作者：jerry，时间：2026-03-26）
- **类型**：Perf/Refactor
- **摘要**：将 `rowsPerShard` 从软约束改为硬约束，删除 `GlobalIndexBuilderUtils` 中冗余逻辑，精简全局索引构建的 Topo 构建器。
- **关键文件**：
  - `paimon-core/.../globalindex/GlobalIndexBuilderUtils.java`：删除冗余逻辑
  - `paimon-flink/.../globalindex/GenericIndexTopoBuilder.java`：精简实现

---

### 24. Other | release | 文档更新（3条，简略）

- **commit**：`a219294` — 新增 Global Index 文档页
- **commit**：`e109ccd` — 更新索引文档，不再仅限实时数据湖场景
- **commit**：`c96c8de` — 更新 Append Table 文档结构

---

### 25. Other | release | pypaimon 发布合规（LICENSE/NOTICE）

- **commit**：`0c46536`（作者：Jingsong Lee，时间：2026-03-26）
- **类型**：Other
- **摘要**：在 sdist 中包含 LICENSE 和 NOTICE 文件，满足 Apache 发布合规要求。

---

### 26. Fix | core | SimpleLsmKvDb 文件路径加入 UUID 防止多 Bucket 冲突

- **commit**：`8c467ff`（作者：Jingsong Lee，时间：2026-03-26）
- **类型**：Fix
- **摘要**：当 bucket > 1 时，多个 `ClusteringCompactManager` 实例可能共享同一临时目录，原 SST 文件使用顺序编号（sst-000000.db）导致相互覆盖，引发主键去重失败。修复：SST 文件名加入 UUID，确保唯一性。
- **关键文件**：
  - `paimon-core/.../lookup/sort/db/SimpleLsmKvDb.java`：SST 文件名加 UUID
  - `paimon-core/.../disk/IOManagerImpl.java`：配合调整

---

### 27. Fix | core | ClusteringCompactManager 清理过期 DV 文件

- **commit**：`58188b7`（作者：Jingsong Lee，时间：2026-03-26）
- **类型**：Fix
- **摘要**：`ClusteringCompactManager` 在压缩后未清理已过期的 Deletion Vector 文件，本次修复补充清理逻辑，防止 DV 文件泄漏。
- **关键文件**：
  - `paimon-core/.../clustering/ClusteringCompactManager.java`：补充 DV 文件清理

---

### 28. Feature | spark | Spark SQL 支持读取 ObjectTable

- **commit**：`024f4c7`（作者：Zouxxyy，时间：2026-03-26）
- **类型**：Feature
- **摘要**：为 `SparkObjectTable` 实现 `SupportsRead` 接口，新增 `ObjectTableScan` / `ObjectTableScanBuilder`，使 Spark SQL 能够直接查询 ObjectTable 中的对象数据。
- **关键文件**：
  - `paimon-spark/.../spark/read/ObjectTableScan.scala`：新增 ObjectTable 扫描实现（52行）
  - `paimon-spark/.../spark/SparkTable.scala`：接入 SupportsRead

---

### 29. Feature | spark | REST Catalog 场景下 Merge Schema 路由通过 Catalog

- **commit**：`6df058e`（作者：Zouxxyy，时间：2026-03-26）
- **类型**：Fix/Feature
- **摘要**：使用 REST Catalog 时，写入时的 Schema Merge 原本绕过 Catalog 直接提交到 `SchemaManager`，在 REST Catalog 下无效。本次引入 `SchemaModification` 将 Schema 变更路由通过 `Catalog.alterTable()`，并新增 `diffSchemaChanges()` 方法转换 Schema 差异为 `SchemaChange` 列表。
- **关键文件**：
  - `paimon-core/.../table/SchemaModification.java`：新增 Schema 变更包装类（46行）
  - `paimon-core/.../schema/SchemaMergingUtils.java`：新增 diff 计算方法
  - `paimon-spark/.../WriteMergeSchemaWithRestCatalogTest.scala`：新增集成测试（74行）

---

### 30. Fix | core | 修复流式写入 pk-clustering-override 时频繁触发压缩

- **commit**：`16ecc13`（作者：Jingsong Lee，时间：2026-03-26）
- **类型**：Fix
- **摘要**：`pk-clustering-override` 场景下流式写入时，压缩被频繁触发，本次在 `ClusteringCompactManager` 中修复触发条件判断逻辑。
- **关键文件**：
  - `paimon-core/.../clustering/ClusteringCompactManager.java`：修复触发条件

---

## 模块统计

| 模块 | 新增 commit 数 | 类型分布 |
|------|--------------|---------|
| core | 17 | Feature×7, Fix×7, Perf×2, Refactor×1 |
| python | 7 | Feature×3, Fix×3, Refactor×1 |
| spark | 5 | Feature×3, Fix×2 |
| flink/spark | 1 | Feature×1 |
| release | 3 | Release×3 |
| doc | 3 | Docs×3 |

> **本轮核心主题**：全局索引（Global Index）体系大规模重构与增强——向量检索 API 解耦、BTree 索引行范围构建、预过滤机制引入；Chain 表 group partition 支持；Clustering Compact 多项 Bug 修复；Python API 功能补全。
